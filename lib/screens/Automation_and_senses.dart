import 'package:flutter/material.dart';

class AutomationAndSenses extends StatelessWidget {
  const AutomationAndSenses({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
